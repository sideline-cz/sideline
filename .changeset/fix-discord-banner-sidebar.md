---
'@sideline/web': patch
---

Render the pending Discord-join banner inside the sidebar inset instead of above the layout. The fixed-position sidebar was overlapping the banner's left half, clipping the message.
