---
'@sideline/domain': patch
'@sideline/migrations': patch
'@sideline/server': patch
'@sideline/web': patch
---

Captains can now create, update, and delete custom activity types (previously admin-only). The activity types management page is now reachable from the team sidebar (under Coach).
